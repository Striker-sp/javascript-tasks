function myFunction(msg){   //(msg) is a parameter as in contains something which gets called while calling a function
    // paramerer -> input
    console.log(msg);
}

myFunction("Good Coding..."); //argument